<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="GroupBoxStyle" TargetType="{x:Type GroupBox}">
        <Setter Property="Margin" Value="5" />
    </Style>

    <Style x:Key="GridLabelStyle" TargetType="{x:Type Label}">
        <Setter Property="Margin" Value="5" />
    </Style>

    <Style x:Key="GridTextBoxStyle" TargetType="{x:Type TextBox}">
        <Setter Property="Margin" Value="5" />
    </Style>

    <Style x:Key="SpecityButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Margin" Value="5" />
    </Style>

    <Style x:Key="LaunchButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Margin" Value="5" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="HorizontalAlignment" Value="Left" />
    </Style>
</ResourceDictionary>
